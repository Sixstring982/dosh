.container
  .row
    .col.l6.offset-l3.s12
      .card.white.black-text
        .card-content
          %span.card-title New Account
          = render "form.html", account_map: @account_map, user_id: @user_id, changeset: @changeset, action: account_path(@conn, :create)
        .card-action
          = link "Back", to: account_path(@conn, :index)
