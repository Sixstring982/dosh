.container
  .row
    .col.l6.offset-l3.s12
      .card.white.black-text
        .card-content
          %span.card-title Edit Account
          = render "form.html", account_map: @account_map, user_id: @user_id, changeset: @changeset, action: account_path(@conn, :update, @account)
          = link "Back", to: page_path(@conn, :index)
